# 🚀 คู่มือการติดตั้ง - AI VTuber Core

เอกสารนี้จะแนะนำขั้นตอนการติดตั้งและเริ่มต้นใช้งาน **AI VTuber Core CLI Demo** บนระบบปฏิบัติการ **Windows**

---

## 📋 สิ่งที่ต้องมี (Prerequisites)

1.  **Python 3.10+:**
    *   ดาวน์โหลดได้ที่ [python.org](https://www.python.org/downloads/)
    *   **สำคัญ:** ตอนติดตั้ง ให้ติ๊ก ✅ `Add Python to PATH`

2.  **Git:**
    *   ดาวน์โหลดได้ที่ [git-scm.com](https://git-scm.com/download/win)

3.  **(ทางเลือก) LLM API Keys:**
    *   **OpenAI:** สำหรับ GPT-4 (ต้องมีเครดิต)
    *   **Google Gemini:** รับ API Key ฟรีได้ที่ [ai.google.dev](https://ai.google.dev)

4.  **(ทางเลือก) Ollama (สำหรับ LLM Local):**
    *   ถ้าต้องการรันโมเดลภาษาบนเครื่องของคุณเอง (ไม่ต้องใช้ API key)
    *   ดาวน์โหลดได้ที่ [ollama.com](https://ollama.com/download)
    *   หลังจากติดตั้ง ให้รันคำสั่ง: `ollama pull llama3.2:3b`

---

## ⚙️ ขั้นตอนการติดตั้ง

### 1. Clone Repository

เปิด Command Prompt (CMD) หรือ PowerShell แล้วรันคำสั่ง:

```bash
git clone https://github.com/intity01/sakusama.git
cd sakusama/ai-vtuber-core
```

### 2. สร้าง Virtual Environment

Virtual Environment (venv) ช่วยแยกสภาพแวดล้อมของโปรเจกต์นี้ออกจาก Python หลักของคุณ

```bash
python -m venv venv
```

### 3. เปิดใช้งาน Virtual Environment

คุณต้องเปิดใช้งาน venv **ทุกครั้ง** ที่จะรันโปรเจกต์นี้

```bash
.\venv\Scripts\activate
```

หลังจากรันคำสั่งนี้ คุณจะเห็น `(venv)` นำหน้า command prompt ของคุณ

### 4. ติดตั้ง Dependencies

ติดตั้งไลบรารีที่จำเป็นทั้งหมดจากไฟล์ `requirements.txt`

```bash
pip install -r requirements.txt
```

ขั้นตอนนี้อาจใช้เวลาสักครู่

---

## 🔧 การตั้งค่า (Configuration)

### 1. คัดลอกไฟล์ Config ตัวอย่าง

```bash
copy config\config.example.json config\config.json
```

### 2. แก้ไข `config.json`

เปิดไฟล์ `config/config.json` ด้วย text editor (เช่น Notepad, VS Code) แล้วแก้ไขตามต้องการ

#### การตั้งค่า LLM (สำคัญ!)

-   **provider:** เลือกผู้ให้บริการ LLM ที่จะใช้เป็นค่าเริ่มต้น (`openai`, `ollama`, หรือ `gemini`)
-   **api_key:** ใส่ API key ของคุณ (สำหรับ `openai` และ `gemini`)
-   **model:** เลือกรุ่นของโมเดลที่ต้องการใช้

```json
{
  "llm": {
    "provider": "ollama", // "openai", "gemini"
    "models": {
      "openai": {
        "api_key": "sk-YOUR_OPENAI_KEY",
        "model": "gpt-4.1-mini"
      },
      "gemini": {
        "api_key": "YOUR_GEMINI_API_KEY",
        "model": "gemini-1.5-flash-latest"
      },
      "ollama": {
        "base_url": "http://localhost:11434",
        "model": "llama3.2:3b"
      }
    }
  }
}
```

#### การตั้งค่า TTS (Text-to-Speech)

-   **enabled:** `true` เพื่อเปิดใช้งานเสียงพูด
-   **engine:** `pyttsx3` (แนะนำสำหรับ Windows, ทำงาน offline) หรือ `edge` (คุณภาพสูง, ต้องการอินเทอร์เน็ต)
-   **voice_index:** เลขดัชนีของเสียงใน Windows (ดูวิธีหาได้ใน `TTS_GUIDE.md`)

```json
{
  "tts": {
    "enabled": true,
    "engine": "pyttsx3",
    "rate": 150,
    "volume": 0.9,
    "voice_index": 0, // 0 คือเสียง default
    "edge_voice": "th-TH-PremwadeeNeural"
  }
}
```

---

## ▶️ การรัน Demo

หลังจากตั้งค่าเสร็จแล้ว ให้รันไฟล์ `demo_cli.py`

```bash
python demo_cli.py
```

โปรแกรมจะให้คุณเลือก:
1.  **LLM Provider:** ผู้ให้บริการโมเดลภาษา
2.  **Persona:** บุคลิกของ AI

จากนั้นคุณก็สามารถเริ่มพูดคุยกับ AI ได้เลย!

---

## ❌ การแก้ไขปัญหา (Troubleshooting)

-   **`'python' is not recognized...`:**
    *   คุณอาจจะลืมติ๊ก `Add Python to PATH` ตอนติดตั้ง Python ให้ติดตั้งใหม่อีกครั้ง

-   **`No module named '...'`:**
    *   คุณอาจจะลืมเปิดใช้งาน Virtual Environment (`.\venv\Scripts\activate`) หรือลืมติดตั้ง dependencies (`pip install -r requirements.txt`)

-   **ไม่มีเสียงพูด:**
    *   ตรวจสอบว่า `tts.enabled` เป็น `true` ใน `config.json`
    *   ตรวจสอบการตั้งค่าเสียงใน Windows
    *   อ่านรายละเอียดเพิ่มเติมที่ `TTS_GUIDE.md`
