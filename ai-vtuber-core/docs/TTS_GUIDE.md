_# 🔊 คู่มือการใช้งาน TTS (Text-to-Speech)_

เอกสารนี้อธิบายการทำงานของระบบ Text-to-Speech (TTS) ใน **AI VTuber Core**, วิธีการตั้งค่า, และแนวทางการแก้ไขปัญหาที่พบบ่อย

---

## ⚙️ TTS Engines ที่รองรับ

โปรเจกต์นี้รองรับ TTS Engine 2 ตัว ซึ่งมีข้อดี-ข้อเสียแตกต่างกัน:

| Engine | คำอธิบาย | ข้อดี | ข้อเสีย |
|---|---|---|---|
| `pyttsx3` | **(แนะนำ)** ใช้ System TTS ของ OS | ✅ **ทำงาน Offline**<br>✅ ไม่ต้องใช้ API Key<br>✅ ติดตั้งง่าย | ❌ คุณภาพเสียงขึ้นอยู่กับ OS<br>❌ ต้องตั้งค่า `voice_index` เอง |
| `edge-tts` | ใช้ Microsoft Edge TTS (Cloud) | ✅ **คุณภาพเสียงสูงมาก**<br>✅ เสียงเป็นธรรมชาติ | ❌ **ต้องใช้อินเทอร์เน็ต**<br>❌ อาจมี Latency เล็กน้อย |

**สรุป:** สำหรับการใช้งานทั่วไปบน **Windows**, แนะนำให้ใช้ `pyttsx3` เป็นค่าเริ่มต้น

---

## 🔧 การตั้งค่าใน `config.json`

คุณสามารถตั้งค่า TTS ได้ในไฟล์ `config/config.json`

```json
"tts": {
  "enabled": true,         // เปิด/ปิดระบบ TTS
  "engine": "pyttsx3",       // เลือก engine: "pyttsx3" หรือ "edge"
  
  // --- การตั้งค่าสำหรับ pyttsx3 ---
  "rate": 150,             // ความเร็วในการพูด (คำต่อนาที)
  "volume": 0.9,           // ความดัง (0.0 - 1.0)
  "voice_index": 0,        // ดัชนีเสียงของ Windows (ดูวิธีหาด้านล่าง)
  
  // --- การตั้งค่าสำหรับ edge-tts ---
  "edge_voice": "th-TH-PremwadeeNeural", // เสียงของ Edge TTS (ดูชื่อเสียงทั้งหมดได้ที่ [1])
  
  // --- การตั้งค่า Cache ---
  "cache_enabled": true    // เปิด/ปิดการ cache ไฟล์เสียง (สำหรับ edge-tts)
}
```

---

## 🎤 (สำคัญ!) วิธีหา `voice_index` สำหรับ `pyttsx3` บน Windows

`pyttsx3` ใช้เสียงที่ติดตั้งอยู่ใน Windows ของคุณ แต่ละเสียงจะมี "ดัชนี" (index) เป็นตัวเลข คุณต้องหาเลขดัชนีของเสียงที่คุณต้องการ (โดยเฉพาะเสียงภาษาไทย) เพื่อนำไปใส่ใน `config.json`

### ขั้นตอนการหา Voice Index

1.  **สร้างไฟล์ Python:** สร้างไฟล์ชื่อ `find_voices.py` ในโฟลเดอร์ `ai-vtuber-core` แล้วคัดลอกโค้ดด้านล่างไปใส่

    ```python
    # find_voices.py
    import pyttsx3

    try:
        engine = pyttsx3.init()
        voices = engine.getProperty('voices')

        print("="*50)
        print("🎤 Available TTS Voices on this system")
        print("="*50)

        for i, voice in enumerate(voices):
            print(f"Index: {i}")
            print(f"  ID:   {voice.id}")
            print(f"  Name: {voice.name}")
            print(f"  Lang: {voice.languages}")
            print("-" * 20)

        print("="*50)
        print("📌 Copy the 'Index' number of your desired voice")
        print("   and paste it into 'voice_index' in config.json")
        print("="*50)

    except Exception as e:
        print(f"❌ Error: Could not initialize TTS engine.")
        print(f"   Make sure you have a TTS engine installed on your OS.")
        print(f"   Error details: {e}")

    ```

2.  **รันสคริปต์:** เปิด Command Prompt, เปิดใช้งาน Virtual Environment (`.\venv\Scripts\activate`), แล้วรันคำสั่ง:

    ```bash
    python find_voices.py
    ```

3.  **ดูผลลัพธ์:** สคริปต์จะแสดงรายการเสียงทั้งหมดที่มีในเครื่องของคุณ พร้อมเลข `Index`

    ```text
    ==================================================
    🎤 Available TTS Voices on this system
    ==================================================
    Index: 0
      ID:   HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Speech\Voices\Tokens\TTS_MS_EN-US_DAVID_11.0
      Name: Microsoft David Desktop - English (United States)
      Lang: ['en_US']
    --------------------
    Index: 1
      ID:   HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Speech\Voices\Tokens\TTS_MS_TH-TH_PATTARA_11.0
      Name: Microsoft Pattara Desktop - Thai
      Lang: ['th_TH']
    --------------------
    ==================================================
    📌 Copy the 'Index' number of your desired voice
       and paste it into 'voice_index' in config.json
    ==================================================
    ```

4.  **นำ Index ไปใส่ใน Config:** จากตัวอย่างด้านบน ถ้าคุณต้องการใช้เสียง **Microsoft Pattara (ภาษาไทย)**, `Index` คือ `1`

    ให้คุณไปแก้ `config.json` ดังนี้:

    ```json
    "tts": {
      "voice_index": 1
    }
    ```

---

## ❌ การแก้ไขปัญหา (Troubleshooting)

#### 1. ไม่มีเสียงพูดเลย

-   **ตรวจสอบ `config.json`:**
    *   `"enabled": true` หรือไม่?
-   **ตรวจสอบ Volume:**
    *   เช็ค Volume ใน Windows และในโปรแกรมอื่นๆ ว่ามีเสียงหรือไม่
-   **ลองเปลี่ยน Engine:**
    *   ลองสลับระหว่าง `pyttsx3` และ `edge-tts` เพื่อดูว่ามีอันไหนทำงานได้หรือไม่
-   **สำหรับ `pyttsx3`:**
    *   `voice_index` ถูกต้องหรือไม่? ลองใช้ `0` (เสียง default) ดูก่อน
    *   Windows มีเสียงติดตั้งไว้หรือไม่? (ไปที่ Settings > Time & Language > Speech)
-   **สำหรับ `edge-tts`:**
    *   เครื่องคุณเชื่อมต่ออินเทอร์เน็ตหรือไม่?

#### 2. ได้ยินเสียง แต่เป็นภาษาอังกฤษ (ทั้งที่ตั้งค่าเสียงไทยแล้ว)

-   ปัญหานี้มักเกิดกับ `pyttsx3`
-   **สาเหตุ:** `voice_index` ที่ใส่ใน `config.json` ไม่ถูกต้อง หรือเป็นเสียงภาษาอังกฤษ
-   **วิธีแก้:** รันสคริปต์ `find_voices.py` อีกครั้งเพื่อหา Index ของเสียงภาษาไทยที่ถูกต้อง

#### 3. Error: `No module named 'pyttsx3'` หรือ `'edge_tts'`

-   **สาเหตุ:** คุณอาจจะลืมเปิดใช้งาน Virtual Environment หรือติดตั้ง dependencies
-   **วิธีแก้:**
    1.  รัน `.\venv\Scripts\activate`
    2.  รัน `pip install -r requirements.txt`

#### 4. Error: `pyttsx3.init() failed`

-   **สาเหตุ:** เกิดขึ้นได้ยากบน Windows แต่อาจเกิดในสภาพแวดล้อมที่ไม่มี Audio System (เช่น ใน Server หรือ Docker container บางตัว)
-   **วิธีแก้:** โปรเจกต์นี้ออกแบบมาเพื่อรันบน Desktop ที่มี Audio System อยู่แล้ว ปัญหานี้ไม่ควรเกิดขึ้นในการใช้งานปกติ

---

### อ้างอิง

[1] [Microsoft Edge TTS - Voice List](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/language-support?tabs=tts#text-to-speech)
