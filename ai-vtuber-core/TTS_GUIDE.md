# TTS (Text-to-Speech) Guide

## 🎤 ภาพรวม

ตอนนี้ AI VTuber สามารถ**พูดตอบเป็นเสียง**ได้แล้ว! ใช้ **Microsoft Edge TTS** ซึ่งฟรี 100% และมีเสียงภาษาไทยคุณภาพสูง

---

## ✨ คุณสมบัติ

- ✅ **ฟรี 100%** - ไม่ต้องใช้ API key
- ✅ **เสียงไทยคุณภาพสูง** - มีหลายเสียงให้เลือก
- ✅ **Auto-play** - เล่นเสียงอัตโนมัติ
- ✅ **Cache** - บันทึกเสียงไว้ใช้ซ้ำ (ประหยัดเวลา)
- ✅ **เปิด/ปิดได้** - พิมพ์ `tts` เพื่อสลับ

---

## 📦 การติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
pip install edge-tts pygame
```

หรือ

```bash
pip install -r requirements.txt
```

### 2. วางไฟล์ที่อัปเดต

แตก `ai-vtuber-tts-update.zip` แล้ววางทับไฟล์เดิม:
- `src/tts/` (โฟลเดอร์ใหม่)
- `demo_cli.py`
- `config/config.json`
- `requirements.txt`

---

## 🚀 การใช้งาน

### รันโปรแกรม

```bash
python demo_cli.py
```

### คำสั่งใหม่

- `tts` - เปิด/ปิด TTS
- คำสั่งเดิมยังใช้ได้: `quit`, `persona`, `provider`, `history`, `clear`

### ตัวอย่างการใช้งาน

```
👤 You: สวัสดี

⏳ Thinking...
🤖 AI (warm) [ollama]: สวัสดีค่ะพี่ ซากุมาแล้วนะคะ~
   💭 Tokens: 42
   🔊 Speaking...
```

Saku จะพูดตอบเป็นเสียงไทยอัตโนมัติ!

---

## ⚙️ การตั้งค่า

แก้ไขไฟล์ `config/config.json`:

```json
{
  "tts": {
    "enabled": true,
    "voice": "th-TH-PremwadeeNeural",
    "auto_play": true,
    "cache_enabled": true
  }
}
```

### ตัวเลือก

| ตัวเลือก | ค่าเริ่มต้น | คำอธิบาย |
|----------|-------------|----------|
| `enabled` | `true` | เปิด/ปิด TTS |
| `voice` | `th-TH-PremwadeeNeural` | เสียงที่ใช้ |
| `auto_play` | `true` | เล่นเสียงอัตโนมัติ |
| `cache_enabled` | `true` | บันทึกเสียงไว้ใช้ซ้ำ |

---

## 🎙️ เสียงภาษาไทยที่แนะนำ

| ชื่อ | Voice ID | เพศ | ลักษณะ |
|------|----------|-----|--------|
| **Premwadee** | `th-TH-PremwadeeNeural` | หญิง | เป็นกันเอง น่ารัก (แนะนำสำหรับ Saku) |
| **Achara** | `th-TH-AcharaNeural` | หญิง | มืออาชีพ ชัดเจน |
| **Niwat** | `th-TH-NiwatNeural` | ชาย | ชัดเจน เป็นทางการ |

### วิธีเปลี่ยนเสียง

แก้ไข `config/config.json`:

```json
{
  "tts": {
    "voice": "th-TH-AcharaNeural"
  }
}
```

---

## 🔧 การแก้ปัญหา

### ปัญหา: ไม่มีเสียง

**สาเหตุ:** pygame ไม่ได้ติดตั้งหรือ audio driver มีปัญหา

**วิธีแก้:**
1. ติดตั้ง pygame: `pip install pygame`
2. ตรวจสอบ audio driver ของระบบ
3. ลองปิดเปิดโปรแกรมใหม่

### ปัญหา: Error "edge-tts not installed"

**วิธีแก้:**
```bash
pip install edge-tts
```

### ปัญหา: เสียงช้า/สะดุด

**วิธีแก้:**
1. เปิด cache: `"cache_enabled": true`
2. ครั้งแรกจะช้า (ต้องดาวน์โหลด) แต่ครั้งต่อไปจะเร็ว

### ปัญหา: ต้องการปิด TTS ชั่วคราว

**วิธีแก้:**
- พิมพ์ `tts` ในขณะแชท เพื่อเปิด/ปิด

---

## 📂 โครงสร้างไฟล์

```
ai-vtuber-core/
├── src/
│   └── tts/
│       ├── __init__.py
│       └── tts_module.py       # TTS module หลัก
├── data/
│   └── tts_cache/              # เสียงที่ cache ไว้
├── config/
│   └── config.json             # การตั้งค่า TTS
└── demo_cli.py                 # CLI ที่รองรับ TTS
```

---

## 🎯 ขั้นตอนถัดไป

หลังจากมี TTS แล้ว คุณสามารถพัฒนาต่อได้:

1. **STT (Speech-to-Text)** - พูดกับ Saku ได้
2. **Avatar (Live2D/VRM)** - มีตัวการ์ตูนแสดงอารมณ์
3. **Desktop UI** - สร้าง app สวยๆ

---

## 💡 Tips

- **Cache ช่วยประหยัดเวลา** - ข้อความเดิมจะเล่นเสียงเดิมทันที
- **ปิด TTS ตอนทดสอบ** - พิมพ์ `tts` เพื่อปิดชั่วคราว
- **ลองเสียงต่างๆ** - แต่ละเสียงมีบุคลิกต่างกัน

---

**เวอร์ชัน:** 0.3.0  
**วันที่:** 16 พฤศจิกายน 2025
